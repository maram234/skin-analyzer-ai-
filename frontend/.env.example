# API base URL (no trailing slash).
# Default in code: https://maramm12-skin-analyzer-api.hf.space
# Local dev: set to http://127.0.0.1:8000
# Vercel: leave unset to use HF Space, or override if needed
VITE_API_URL=http://127.0.0.1:8000
